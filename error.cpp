//compile time error
//runtime erro
//warnings
//
//compile time error ->c++(with some problem) -> compiler -> will not run show you the error and tell you remove there  will not compile
//run time rror -> it will compile it will not goona do what you wanted it do  and sometime is runtime error failed lead program to crash
//warning --> its nt gooona halt the prgram but tell you what you are doing is problematic 
//
#include <iostream>

int main(){
	std::cout<<"hello world" << std::endl; //will show no semi colen
		// run time error
		7/0;
		return 0;
}
