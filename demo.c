/*  double quote "" are not sring byut array
  %d  -> number like 42 ||     %.1f -> floating number like 3.5  || %s -> sarah character need to terminated array which at the 0 at the end
 	
 *  */

#include <stdio.h>
	int main(){
		int usf,euf;
		printf("Enter US Floor \n");
		scanf("%d", &usf);
		euf = usf-1;
		printf("EU Floor %d\n", euf);



}
